<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_grap"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.first.vdemo.ui.HomeFragment"
        android:label="fragment_home"
        tools:layout="@layout/fragment_home" >
        <action
            android:id="@+id/action_homeFragment_to_myselfFragment"
            app:destination="@id/myselfFragment" />
    </fragment>
    <fragment
        android:id="@+id/myselfFragment"
        android:name="com.first.vdemo.ui.MyselfFragment"
        android:label="fragment_myself"
        tools:layout="@layout/fragment_myself" />
</navigation>